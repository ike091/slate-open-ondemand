apiVersion: v1
kind: ConfigMap
data:
  clusters.yaml: |
    ---
    v2:
      metadata:
        title: "Kingspeak"
        priority: 2
      login:
        host: "kingspeak.chpc.utah.edu"
metadata:
  name: {{ include "open-ondemand.fullname" . }}-shell-setup